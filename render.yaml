# Render Deployment Configuration
# Docs: https://render.com/docs/yaml-spec

services:
  - type: web
    name: raggita-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port $PORT --workers 1"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.12"
      # Groq LLM Configuration
      - key: OPENAI_API_KEY
        sync: false  # Set this manually in Render dashboard (your Groq API key)
      - key: GROQ_API_KEY
        sync: false  # Set this manually in Render dashboard
      - key: OPENAI_API_BASE
        value: "https://api.groq.com/openai/v1"
      - key: LLM_MODEL
        value: "llama-3.1-8b-instant"
      # Force local embeddings to match FAISS DB dimensions
      - key: FORCE_LOCAL_EMBEDDINGS
        value: "true"
      # Disable Bytez (using Groq instead)
      - key: USE_BYTEZ
        value: "false"
    healthCheckPath: /health
    autoDeploy: true
    plan: free  # Change to 'starter' or 'standard' for production
